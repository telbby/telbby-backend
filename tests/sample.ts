/**
 * 이 모듈은 테스트 샘플 코드를 위한 모듈입니다.
 * 실제 테스트 코드 작성 시에는 src 디렉토리 내의 모듈들을 테스트하는 코드를 작성합니다.
 * tests 디렉토리에는 테스트 코드들만 위치시키도록 합니다.
 */

function add(a: number, b: number): number {
  return a + b;
}

export default add;
